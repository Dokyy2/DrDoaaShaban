# تشغيل نظام الإدارة والحجز

## 1. إنشاء المشروع

أنشئ مشروعًا جديدًا في Supabase، ثم افتح **SQL Editor** ونفّذ محتوى `supabase/schema.sql` بالكامل مرة واحدة.

إذا كان المشروع والـSQL موجودين بالفعل، لا تعيدي تشغيل `schema.sql`؛ نفّذي بدلًا منه ملف `supabase/security_migration.sql` مرة واحدة.

## ترقيات المشروع الحالية

لإضافة دفتر المريضات والتنبيهات شغّلي بالترتيب: `supabase/01_add_enum_values.sql` ثم `supabase/patient_followup_migration.sql`.

ولإضافة عائلة العيادة والحسابات والأسعار ووسائل الدفع والتقفيل اليومي شغّلي بعدها: `supabase/03_family_and_finance_migration.sql`.

بعدها تدخل الدكتورة إلى **إعدادات العيادة** وتختار «تشغيل نظام الحسابات» عند الرغبة. يمكن ترك الأسعار صفرًا وتشغيله لاحقًا دون التأثير على الحجوزات أو بيانات المريضات.

تحديث المرحلة الثالثة يضيف أيضًا: وقت الموعد وقائمة الانتظار، سبب الإلغاء أو عدم الحضور، سجلًا تلقائيًا لتعديلات الدفع، ومصروفات العيادة للدكتورة فقط. شغّلي الملف كاملًا عند وصول نسخة محدثة منه؛ لا يحذف حجوزات أو بيانات مريضات.

في النسخة الحالية يضيف الملف كذلك ملاحظات التواصل، عدّاد الولادات مع الدكتورة، و`finance_start_date`؛ لذلك يبدأ التقفيل والحسابات من يوم تنفيذ الترقية ولا يضيف أي تحصيلات تاريخية قديمة.

## 2. حسابات الدخول

من **Authentication → Users** أنشئ حسابين ببريد إلكتروني وكلمة مرور قوية: حساب السكرتارية وحساب الدكتورة. من **Authentication → Providers → Email** أوقفي خيار السماح بالتسجيلات الجديدة (Enable signups)، حتى لا يستطيع أي شخص إنشاء حساب موظف بنفسه.

كل حساب جديد يصبح حالته `pending` ولا يرى بيانات العيادة. بعد إنشاء الحسابين يدويًا، حدّث حساب الدكتورة في SQL Editor:

```sql
update public.profiles
set role = 'doctor', full_name = 'د. دعاء شعبان'
where id = '<USER_ID>';
```

حدّث الاسم لحساب السكرتارية بالطريقة نفسها عند الحاجة. لا تنشئ الحسابات من صفحة الموقع العامة.

## 3. ربط الموقع

انسخ Project URL وAnon key من **Project Settings → API** إلى `js/config.js`:

```js
window.CLINIC_SUPABASE = {
  url: 'https://YOUR-PROJECT.supabase.co',
  anonKey: 'YOUR-ANON-KEY'
};
```

بعد الحفظ، يبدأ الموقع العام في تسجيل الحجوزات في قاعدة البيانات الجديدة، وتعمل لوحة الإدارة من `dashboard/index.html`.

## 4. فتح لوحة الإدارة

بعد رفع الموقع على الاستضافة، ارفعي **المجلدات كاملة بنفس ترتيب المشروع**، وبالأخص:

- `dashboard/` كاملًا (ويحتوي `index.html` و`dashboard.js` و`dashboard.css`)
- `js/config.js`

لو كان رابط الموقع العام مثل `https://example.com/`، فرابط اللوحة هو:

```text
https://example.com/dashboard/
```

لا تحتاج اللوحة إلى رابط أو مفتاح مختلف؛ هي تقرأ تلقائيًا نفس إعدادات Supabase من `js/config.js`. إذا ظلت عبارة «جارٍ تجهيز لوحة الإدارة» ظاهرة، فهذه علامة أن ملف `dashboard.js` لم يُرفع أو لا يمكن تحميله من الاستضافة.

## 4. ما يحدث قبل الربط

إذا بقي ملف الإعدادات فارغًا، يستمر الموقع في استخدام Google Apps Script الحالي للحجز. تظهر لوحة الإدارة رسالة إعداد بدل عرض أي بيانات. لا توجد حالة نجاح وهمية عند تعذر الاتصال.

## ملاحظات تشغيلية

- المفتاح `anon` ليس مفتاحًا سريًا؛ الحماية تعتمد على سياسات RLS الموجودة في الملف.
- لا تشارك Service Role key أو تضعه في أي ملف داخل الموقع.
- المرفقات الجديدة تُحفظ بصورة محمية داخل سجل الحجز وتظهر في ملف المريضة للموظفات المسجلات فقط. الحد الأقصى الحالي للملف 5 ميجابايت.
- التنبيهات الذكية تعمل طالما لوحة الإدارة مفتوحة: تذكير قبل الموعد حتى 30 دقيقة، ثم تنبيه متابعة بعده بـ15 دقيقة إذا لم يُسجل إجراء. يمكن تأجيل التنبيه دقيقة من النافذة نفسها.
- قبل استخدام أزرار QR للدفع، استبدلي صور الرموز التجريبية داخل `assets/payment-qr/` بالرموز الحقيقية الخاصة بالعيادة، مع الإبقاء على الأسماء نفسها.
