# قواعد أمان مشروع العيادة

## ما يمكن رفعه إلى GitHub

- ملفات الموقع، لوحة الإدارة، وملفات SQL الموجودة في مجلد `supabase`.
- مفتاح Supabase الذي يبدأ بـ `sb_publishable_` فقط. هذا مفتاح عام مخصص للمتصفح؛ حماية البيانات تعتمد على سياسات RLS في قاعدة البيانات.

## ما لا يُرفع إلى GitHub نهائيًا

- أي مفتاح يبدأ بـ `sb_secret_` أو مفتاح `service_role`.
- كلمات مرور الدكتورة أو السكرتارية أو روابط استعادة الدخول.
- ملفات Excel أو CSV أو النسخ الاحتياطية أو أي ملف يحوي أسماء أو أرقام هواتف أو روشتات.
- صور مرفقات المرضى التي تم تنزيلها من لوحة الإدارة.

## قبل الإطلاق

1. اجعلي مستودع GitHub خاصًا (Private) قدر الإمكان.
2. شغّلي ملفات Supabase الخاصة بالأمان والترقية، وتحققي من Security Advisor داخل Supabase.
3. اجعلي حسابي الدكتورة والسكرتارية فقط بدور `doctor` أو `secretary`؛ أي حساب جديد يجب أن يبقى `pending`.
4. استخدمي كلمة مرور قوية وفريدة لكل حساب، وفعّلي التحقق بخطوتين إن كان متاحًا للحساب.
5. لا تتجاوزي تحذير GitHub عند اكتشاف مفتاح؛ أزيلي المفتاح أو بدّليه أولًا.

## إذا انكشف مفتاح حساس

ألغيه فورًا من Supabase: Settings > API Keys، ثم أنشئي مفتاحًا جديدًا. لا يكفي حذفه من الملف فقط إذا كان قد رُفع إلى GitHub.
